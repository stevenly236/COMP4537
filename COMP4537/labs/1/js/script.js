window.addEventListener('load', () => {
    document.getElementById('notes-title').textContent = MESSAGES.notesTitle;
    document.getElementById('writer-link').textContent = MESSAGES.writerLink;
    document.getElementById('reader-link').textContent = MESSAGES.readerLink;
});
